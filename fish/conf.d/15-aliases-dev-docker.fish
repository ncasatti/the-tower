# ====================
# Docker Development Aliases
# ====================
# Docker and docker-compose shortcuts

# Docker basics
abbr -a dk docker
abbr -a dc 'docker compose'
abbr -a dcu 'docker compose up'
abbr -a dcud 'docker compose up -d'
abbr -a dkb 'docker build'
abbr -a dkr 'docker run'
abbr -a dkp 'docker ps'
abbr -a dkpa 'docker ps --all'
abbr -a dki 'docker images'
abbr -a dks 'docker stop'
abbr -a dke 'docker exec -it'
abbr -a lzd lazydocker
